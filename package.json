{
  "name": "suraga-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:pages": "node scripts/build-pages.mjs",
    "check:generated": "npm run build:pages && git diff --exit-code -- public/index.html public/ar.html public/robots.txt public/sitemap.xml public/config.json",
    "check:js": "node scripts/check-js-syntax.mjs",
    "check": "npm run check:generated && npm run check:js",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "http-server": "^14.1.1"
  }
}
